# 成功大隊勤務看板

顯示當時段的人力、車輛編組與勤務類別，並整合 Google 行事曆行程。

---

## 這套東西怎麼運作

勤務資料鎖在需要登入的消防資訊系統（ttfd2.firemis.tw）裡。瀏覽器有安全機制，
**放在別的網站上的網頁沒辦法直接去抓它**。所以整套拆成三個角色：

```
[隊部電腦，人員已登入 ttfd2]
      │  點一下「更新看板」書籤
      │  在 ttfd2 自己的頁面裡讀取勤務表（同網域，合法）
      ▼
[Render 伺服器]  收下資料，存起來
      │
      ▼
[電視牆 / 手機 / 任何人開網址]   ← 不需要登入，自動更新
```

重點：**抓資料的動作發生在 ttfd2 自己的頁面裡**，所以拿得到；
看板端讀的是 Render 上的資料，所以不用登入。
帳號密碼全程不離開操作人員，不存在任何檔案或伺服器上。

**行事曆是另外一條完全獨立、不需要人操作的管線**：Google 日曆的
iCal 訂閱網址本身就是通行證，Render 伺服器自己每 12 小時去讀一次，
不用登入、不用書籤、不用隊部電腦開著。設定方式見下方部署步驟。

---

## 資料夾說明

| 位置 | 內容 |
|---|---|
| `web/` | 看板網頁本身。部署後電視牆和手機開的就是這個 |
| `server/` | Render 上的服務：收資料、供應資料、排程讀取 Google 行事曆 |
| `collector/` | 隊部電腦用的採集器（書籤／Tampermonkey 自動版）與探測腳本 |
| `docs/` | 給資訊單位或系統廠商的資料介面需求說明 |
| `勤務看板.html` | 單機版看板，雙擊即可開，供版面確認用 |

---

## 部署步驟

### 一、Render 建立服務

1. 把這個資料夾推上 GitHub
2. Render → New → Web Service → 連結該倉庫
3. 設定：
   - **Root Directory**：`server`
   - **Build Command**：`npm install`
   - **Start Command**：`npm start`
4. Environment 新增變數：
   - `PUSH_TOKEN`：自己取一串亂碼，採集器要用同一串
   - `ALLOW_ORIGIN`：`https://ttfd2.firemis.tw`
   - `CAL_ICS_URL_DAJI`（選填）：「大隊」來源日曆的 iCal 訂閱網址
   - `CAL_ICS_URL_YIXIAO`（選填）：「義消」來源日曆的 iCal 訂閱網址
   - `CAL_ICS_URL_JUBENBU`（選填）：「局本部」來源日曆的 iCal 訂閱網址
   - `CONFIG_PASSWORDS_URL`（選填）：密碼分頁發布出來的 CSV 網址
   - `CONFIG_DEPTS_URL`（選填）：單位代碼分頁發布出來的 CSV 網址
   - `CONFIG_NOTICES_URL`（選填）：跑馬燈分頁發布出來的 CSV 網址
   - `CONFIG_CALENDARS_URL`（選填）：行事曆分頁發布出來的 CSV 網址
     （跟上面 `CAL_ICS_URL_*` 是疊加關係，用來讓其他大隊自己上傳
     自己的行事曆，成功大隊現有的行事曆不用搬過去）

   行事曆那三個都是選填、互相獨立，沒設定就用看板內建的靜態快照。
   `CONFIG_*` 那四個也是選填、互相獨立，設定方式見
   `docs/試算表設定說明.md`（含預先整理好的單位代碼對照表，複製貼上
   即可）。
   拿 iCal 訂閱網址的方式：Google 日曆網頁版 → 該日曆「⋮」→
   設定和共用 → 往下捲到「整合設定」→ 複製「私人網址（iCal 格式）」
   （若該日曆本身已設成公開，也可以用「公開網址」，不含密鑰、風險
   較低）。**這個網址等同密碼，只貼進 Render 的環境變數，不要貼進
   任何會進版本庫的檔案或對話紀錄。**

部署完成後會拿到一個網址，例如 `https://duty-board.onrender.com`。

> Render 免費方案在 15 分鐘無人存取時會休眠，再次開啟約需 30 秒喚醒。
> 電視牆整天開著就不會休眠。

### 二、隊部電腦設定採集器

依 `collector/collector-bookmarklet.txt` 的說明，把書籤加到瀏覽器書籤列。
第一次點擊時會詢問 Render 網址與 `PUSH_TOKEN`，輸入後記在該台電腦。

### 三、日常使用

1. 隊部電腦早上登入 ttfd2
2. 開到勤務表列表頁，按一次「查詢」
3. 點一下「更新勤務看板」書籤
4. 狀態視窗保持開著，之後每 5 分鐘自動更新
5. 系統授權過期時狀態視窗會提示，重新登入後再點一次書籤

---

## 目前進度

- [x] 看板版面（依實際勤務表欄位結構）
- [x] Render 服務（收資料 / 供應資料）
- [x] 給資訊單位的資料介面需求說明
- [x] 勤務系統 API 結構釐清（見 `docs/API筆記.md`）
- [x] 採集器（`collector/collector.js`）
- [x] 部署 Render 並實測
- [x] 單位名稱對照修正（改用「主管」欄位內容比對，不受回傳順序影響）
- [x] 當日勤務：依時間排序、已結束項目可摺疊、各單位固定顏色標示
- [x] 資料新鮮度偵測（超過門檻未更新會顯示警示）
- [x] 採集器每天 07:00 額外排程（分頁需持續開啟才有效）
- [x] 日間／夜間模式切換
- [x] 語音提醒（勤務時段前 30 分鐘播報，含播放／暫停按鈕）
- [x] 行事曆自動隱藏已過日期
- [x] 行事曆來源篩選鈕（依大隊／局本部／義消切換）
- [x] 單位視角切換（大隊 / 單一分隊，含語音提醒同步套用）
- [x] 隊徽作為分頁書籤圖示
- [x] 即時出勤狀態（`shift-status/list`，低風險資料，不含案件細節）：
      導覽列下方紅色橫幅，只有真的有人在外時顯示，內容為單位／姓名／
      原因／車輛／起始時間，明確排除請假類項目與案件系統資料
- [x] 即時出勤獨立排程（每 30 分鐘，`server.js` 支援「快速推送」只
      更新這塊、不動勤務表其餘資料），與完整資料的 4 小時排程分開
- [x] 行事曆近 5 天顯示、超過折疊收合，減少版面佔用
- [x] 行事曆每筆項目附「查看完整內容」連結（Google 行事曆原始網址）
- [x] 採集器全自動排程選項（`collector/collector.user.js` +
      `collector/自動化排程說明.txt`）：Tampermonkey 讓腳本在頁面
      開啟時自動執行、搭配 Windows 工作排程器每天定時開啟頁面，
      不需要每天手動點書籤。**不會、也不能自動登入**——登入狀態
      過期時仍需要人工登入一次，這條界線沒有改變
- [x] 修正自動模式單位名稱抓不到的問題：自動觸發時畫面表格常常還沒
      渲染完成，改成輪詢等表格內容連續兩次穩定才讀取（最多等 8 秒），
      不再退回顯示單位代碼
- [x] 修正自動模式取得授權時序問題：自動點「查詢」按鈕時若按鈕還
      沒渲染出來，只檢查一次就放棄會導致誤用錯的授權、打 API 得到
      401，改成一樣用輪詢，最多等 10 秒讓按鈕真的出現再點
- [x] 即時出勤單位名稱改由伺服器每次供應資料時重新校正（見
      `server/server.js` 的 `resolveOutStatusNames()`）：即時出勤
      是任何一個還開著的分頁都可能觸發的「快速推送」，如果剛好是
      舊分頁或時序不順的那次送出，可能帶著錯的單位代碼蓋掉其他
      分頁已經算對的結果；改成伺服器拿「完整推送」裡最新的單位
      對照表重新校正一次，不管哪個分頁、哪個時間點送來的都一樣準
- [x] 舊分頁偵測到新分頁接手會自動停止（多分頁協調）：每個分頁
      啟動時把自己的代號寫進 localStorage，同網域其他分頁偵測到
      代號換人就自動停止排程、嘗試自動關閉，避免 Windows 排程器
      每天開新分頁、舊分頁忘了關時持續浪費資源、洗版狀態視窗
- [x] 修正交接班日期錯位問題（見 `docs/API筆記.md` 第八節）：
      ttfd2 一筆「日期：D」的記錄實際涵蓋 D 08:00 到 D+1 08:00，
      查詢日期若還沒到 08:00 就直接用行事曆日期，會查到還沒開始的
      值班日，看板「目前時段」顯示的其實是明天的班表；改成查詢
      日期會依現在時間自動判斷是否要往前推一天（`dutyDayOf()`），
      每天額外多跑一次的排程時間點也從 07:00 對齊到真正的 08:00。
      **這個修正尚未在真實的 00:00-08:00 時段驗證過，待觀察**
- [x] 伺服器沒有完整資料時自動補跑一次完整採集：電腦待機時分頁不會
      發請求，Render 免費方案太久沒人連線會休眠，醒來後若剛好是
      「即時出勤」30 分鐘排程先觸發，伺服器記憶體是空的、會回報
      「尚未有完整資料」；舊版只會記錄失敗、乾等下次 4 小時排程，
      看板因此可能卡在沒資料狀態長達數小時。改成偵測到這個情況就
      立刻自動補跑一次完整採集，把沒資料的時間縮短到約一分鐘內。
      **注意：這無法讓 Render 完全不休眠，只是縮短恢復時間**
- [x] 全縣多大隊視角（2026-09-12）：局本部帳號能看到的全縣 34 個
      單位（台東大隊、關山大隊、大武大隊、成功大隊、局本部旗下，
      見 `docs/API筆記.md` 第九節的完整對照表），透過另一支完全
      獨立的採集器（`collector/collector-county.js` /
      `collector-county.user.js` / `collector-county-bookmarklet.txt`）
      查詢、推送到 `/api/push-county`，跟成功大隊自己的資料
      （`/api/push`）完全分開存放、互不影響。看板右上角選單新增
      大隊分組（optgroup），選了其他大隊會切換 `UNITS`/`TASKS`/
      `OUT_STATUS` 顯示該大隊的資料；每個大隊底下都能再細看自己
      旗下的單一分隊，跟成功大隊同一套邏輯。全縣採集器需要用
      局本部帳號另外設一組 Tampermonkey + Windows 工作排程器，
      尚未完成自動化排程設定（已用書籤版手動測試成功，34 個單位、
      5 個大隊分組全部正確）
- [x] 修正 `/api/duty` 在成功大隊或全縣資料其中一邊缺席時整包
      404、連累另一邊的問題（實測發生於 Render 重啟後）：改成只有
      兩邊都沒有資料才真的 404，任一邊有資料就正常供應那一邊
- [x] 時段切換選單：獨立於單位範圍之外，可以手動選一個時段固定
      顯示編組卡的值班／車輛內容（例如查「18-19 是誰在班」），
      不用等到真的到那個時間。不影響時鐘、日期、即時出勤橫幅這些
      「現在真的發生什麼事」的即時資訊，一律照真實時間顯示。刻意
      不存進 localStorage——公用電視牆重新整理或隔天應該自動回到
      跟著目前時間走，不讓上一個人選的時段卡住畫面
- [x] Google 行事曆改為伺服器自動排程更新（每 12 小時，見
      `server/calendar.js` + `server.js` 的 `CAL_ICS_URL_*` 環境變數）。
      用日曆的 iCal 訂閱網址讀取，不需要登入、不需要人操作；沒設定
      環境變數時自動退回看板內建的靜態快照。目前已接上「大隊」「義消」
      兩個來源（皆為公開日曆，網址本身不含密鑰）；「局本部」尚未接上，
      需要該日曆擁有者提供私人 iCal 網址才能加入——之後只要多設一個
      `CAL_ICS_URL_JUBENBU` 環境變數即可，不需要改程式碼
- [x] 手動觸發行事曆重新讀取（`/api/cal-refresh?token=PUSH_TOKEN`）：
      剛新增或改了行程、不想等 12 小時自動排程時，瀏覽器開這個網址
      就能立刻觸發一次讀取，不用整個重新部署伺服器
- [x] 密碼保護、單位代碼對照、跑馬燈公告、行事曆來源改用 Google
      試算表管理（2026-09-12，見 `server/sheetConfig.js`、
      `docs/試算表設定說明.md`）：四份資料各自「發布到網路」成 CSV
      連結，伺服器每 30 分鐘自動讀取一次「有哪些來源／內容」這份
      清單，也有手動觸發端點 `/api/config-refresh?token=PUSH_TOKEN`，
      改試算表內容即可生效、不用改程式碼、不用重新部署。
      - **密碼**：只是「防手滑切錯單位」的軟性提醒，不是真的資料
        隔離（`/api/duty` 本來就會把全縣資料一起回應給前端）。密碼
        分頁「大隊」欄可以填大隊名稱，也可以直接填分隊名稱做到分隊
        層級的保護（GATED_NAMES 是同一份清單，前端依目前選的
        brigadeScope／unitScope 分別判斷）；有一組「管理員」密碼可以
        一次解鎖全部大隊與分隊。分隊密碼只鎖「單獨選看該分隊」，選
        該分隊所屬大隊的「全部（大隊視角）」不受影響，仍看得到——
        「大隊視角」是巨觀總覽，不該被底下單一分隊的密碼卡住。成功
        大隊（預設視角）預設不設防，但也可以加密碼保護（2026-09-12
        起支援）——這種情況下，切換視角的下拉選單仍正常可用，只有
        實際勤務內容（總覽帶／編組卡／當日勤務／未到勤／即時出勤／
        跑馬燈）會被 `#lockpanel` 蓋住，顯示「🔒 已設定密碼保護」，
        按按鈕輸入密碼才會顯示；選單裡設了密碼但尚未解鎖的選項後面
        會自動顯示 🔒 提醒。沒有列在密碼分頁的大隊或分隊完全不受
        影響，維持沒有這個功能時的行為。實際密碼只存在伺服器記憶體，
        比對走 `/api/check-brigade-password`，絕不會出現在前端看得到
        的資料裡。切換動作一律直接生效（下拉選單永遠選得動），要不要
        鎖畫面完全交給鎖定畫面判斷，不會在切換當下卡住選單本身。
      - **單位代碼對照**：原本寫死在 `collector-county.js` 的
        `BRIGADE_BY_DEPT` 對照表移到試算表管理，伺服器在組
        `/api/duty` 回應時用試算表資料覆蓋採集器原本算的分類，查不
        到試算表資料時保留採集器原本的結果——之後單位改編、新增
        分隊只要改試算表，不用重新部署、不用重新安裝採集器。
      - **跑馬燈公告**：新增了 `.marquee` 橫幅（在導覽列下方），
        每則公告可指定顯示對象（全部或特定大隊），依目前選的大隊
        視角自動篩選要顯示哪幾則。
      - **行事曆來源**：讓各大隊自己上傳自己的 Google 行事曆，跟
        `CAL_ICS_URL_*` 三個環境變數是疊加關係、不是取代——成功大隊
        現有的那三個行事曆繼續留在環境變數最安全，不用搬進試算表；
        但之後成功大隊想「新增」別的行事曆，或其他大隊想開始自己
        管理行事曆，都直接在試算表加一列就好，不用等重新部署。切換
        大隊視角時，行事曆面板（含篩選鈕、語音提醒）只顯示該大隊
        自己的行事曆內容。
- [x] 修正單位名稱有時候會顯示成代碼的問題（2026-09-14，見
      `collector/collector.js`、`collector-county.js` 及對應
      `.user.js`／書籤版的 `waitForNameMap()`）：單位主管欄位文字要
      跟畫面表格比對才能換成真正的單位名稱，但畫面上的表格是可以
      獨立捲動的區塊，一次只渲染看得到的那幾列——全縣 34 個單位、
      畫面一次通常只顯示十幾列，原本「輪詢等內容穩定」的做法只會
      等到「目前這批」穩定，沒捲到、或還在陸續載入的單位比對不到
      就顯示成單位代碼（實測：34 個單位只有 14 個抓到正確名稱，
      根據使用者提供的真實截圖與採集器紀錄診斷確認）。改成主動找
      表格的可捲動祖先容器、一段一段往下捲、每捲一段就讀取並累加
      結果，直到捲到底且內容連續穩定才判斷讀完；單位數少、不需要
      捲動的情況（例如成功大隊 6 個單位）行為不受影響，且已用模擬
      捲動與延遲載入兩種情境的假資料測試驗證正確。全縣採集器逾時
      上限同時從 10 秒延長到 15 秒，給捲動＋等待多一點餘裕。
- [x] 修正成功大隊視角卡在舊資料、新鮮度時間不會更新的問題
      （2026-09-14，見 `web/index.html` 的 `hqIsFresher()`）：使用者
      改成只跑全縣採集器後回報「採集器內容都正確、也送到伺服器了，
      但前端重新整理，採集時間卻沒有更新」。根因是原本判斷「成功
      大隊該用專屬資料還是借全縣資料」只看專屬採集器
      （`/api/push`）**有沒有**送過資料，不是看**最近有沒有**送過——
      專屬採集器只要曾經成功送過一次，伺服器的 `latest` 就會永遠留著
      （連 Render 重開機都靠 `/tmp` 快取檔存活），之後即使使用者完全
      改用全縣採集器，畫面仍會誤判「成功大隊自己已經有資料」而卡死
      顯示那份早就過時的舊資料與舊時間戳，全縣採集器送再多新資料
      也不會反映在成功大隊視角上。改成兩邊都記錄各自最後送達時間、
      一律比較「誰比較新」而不是「誰存在」，已用模擬新舊時間戳交錯
      的情境測試驗證兩邊都能正確互相取代。

> **2026-09-04 資料誠實性修正**：曾經發現 9/4 之後「大隊」「局本部」
> 標籤的部分行事曆項目，是撰寫時憑經驗編寫、並非真的查詢 Google
> 行事曆取得的內容。已重新完整查詢三個來源日曆並整批替換為真實資料
> （含真實連結），此後 `CAL` 陣列裡的每一筆都應該對應真實查詢結果，
> 不應再手動編寫看起來合理的內容。之後若要延伸快照範圍，務必實際
> 呼叫日曆查詢工具取得資料，不可用「合理推測」代替。

## 開發者備註

`collector/collector.js` 是可讀版；改完後用下列指令重新產生書籤字串：

```bash
python collector/build.py collector/collector.js collector/_min.txt
```

不要用正則 `/\*.*?\*/` 移除區塊註解 —— 程式碼字串裡若含 `*/`（例如 `*/*`）
會被誤判為註解結尾，把中間整段吃掉。`build.py` 已改為逐行判斷。

---

## 長期目標

現行作法仍需人員每日登入一次。最終應請資訊單位或系統廠商開放唯讀資料介面，
屆時看板可完全自動更新、無人操作。需求內容見 `docs/資料介面需求說明.md`。
